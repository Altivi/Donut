<%= div_for post do %>
	<div class="row">
		<div class="col-sm-2">
			<div class="thumbnail">
				<%= image_tag author.avatar.url(:thumb), alt: "Profile photo", class: "img-responsive user-photo" %>
			</div><!-- /thumbnail -->
		</div><!-- /col-sm-1 -->

		<div class="col-sm-10">
			<div class="panel panel-default">
				<div class="panel-heading">
					<strong><%= author.first_name + " " + author.last_name %></strong> 
					<span class="text-muted">
						<% if post.created_at > Time.now.beginning_of_day %>
						    <%="#{time_ago_in_words(post.created_at)} ago"%>
						<% else %>
						    <%= post.created_at.strftime("%b %d, %Y") %>
						<% end %>
					</span>
					<% unless author != current_user %>
						<%= link_to("", [author, post], method: :delete, remote: true, class: "glyphicon glyphicon-remove pull-right", data: { confirm: 'Are you sure?'}) %>
					<% end %>
				</div>
				<div class="panel-body">
					<%= post.content %>
				</div><!-- /panel-body -->
			</div><!-- /panel panel-default -->
		</div><!-- /col-sm-10 -->
	</div><!-- row -->
<% end %>