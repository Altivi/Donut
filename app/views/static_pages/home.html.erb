<% if user_signed_in? %>
  <%= render 'users/user_profile', object: current_user %>
<% else %>
  <div class="center jumbotron">
    <div class="row">
	    <div class="col-md-8">
		    <h1>Welcome to the Donut</h1>

		    <h2>
		      This is the home page
		    </h2>
	     </div>
		 <div class="col-md-4">
		    <h2>Sign up now!</h2>
		    
		    <%= devise_error_messages! %>
	    
			  <div class="panel panel-default">
			     <div class="panel-body">
		        	<%= render 'users/registrations/form' %>
			     </div>
			  </div>
		  </div>
	</div>
  </div>

<% end %>